module.exports = {
  default: {
    require: [
      "./steps/**/*.ts", // Lokalizacja plików kroków (Steps)
      "./hooks/**/*.ts"  // Opcjonalnie: globalne hooki przed/po scenariuszu
    ],
    paths: ["./features/**/*.feature"], // Lokalizacja plików .feature
    format: [
      "summary",                       // Wyświetla podsumowanie w konsoli
      "progress",                      // Pokazuje postęp testów w konsoli
      "json:./reports/cucumber-report.json" // Zapisuje wyniki w formacie JSON
    ],
    publishQuiet: true, // Zapobiega automatycznemu publikowaniu wyników
    parallel: 1, // Możesz zwiększyć liczbę wątków dla równoległego uruchamiania
    requireModule: [
      "ts-node/register" // Obsługuje TypeScript podczas uruchamiania Cucumber.js
    ],
    worldParameters: {
      baseUrl: "http://localhost:8080" // Możesz przekazać globalne zmienne
    }
  }
};
