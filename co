 - script: |
        echo "Executing cleanup of /app/docker/images on remote host as root."
        
        # WAŻNE: To polecenie zostanie wykonane jako root.
        # Upewnij się, że użytkownik, który loguje się przez SSH (ansible_user),
        # ma skonfigurowane sudo bez hasła do wykonania 'sudo su - root' lub podobnego.
        
        ansible all -i '${{ parameters.hostname }},' \
          -m ansible.builtin.shell \
          -a "rm -rf /app/docker/images/*" \
          -e "ansible_user: ${{ parameters.hostname }}-USER ansible_password: ${{ parameters.hostname }}-PASS" \
          --become --become-user=root --become-method=sudo # <<< TUTAJ JEST ZMIANA! >>>

      displayName: 'Clean /app/docker/images on remote host (as root)'
      condition: succeeded() # Wykonaj tylko jeśli poprzednie kroki zakończyły się sukcesem
