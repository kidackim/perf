- name: Copy target.zip from container to remote host
  ansible.builtin.shell: |
    "{{ docker_cmd }}" cp "{{ run_container_detached_result.stdout }}:/app/target.zip" /app/target.zip"
  register: cp_full_target_result
  when: cp_full_target_result is defined and wait_container_result.rc != 0
