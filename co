RUN microdnf update -y && \
    microdnf install -y \
    curl tar gzip xz unzip \ # <--- TUTAJ JEST 'curl'
    ca-certificates openssl \
    libstdc++-devel libgcc_s \
    && microdnf clean all && rm -rf /var/cache/yum/*
