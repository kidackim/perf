- name: Clean up and remove results and target directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  with_items:
    - "{{ base_docker_path }}/results"
    - "{{ base_docker_path }}/target"
  tags:
    - cleanup
