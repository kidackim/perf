ansible-playbook docker_load.yml \
          -i '${{ parameters.hostname }},' \
          -e "ansible_user={{ parameters.hostname }}-USER ansible_password={{ parameters.hostname }}-PASS" \
          -e "build_id_from_pipeline=$(Build.BuildId)"
          # Pamiętaj o bezpieczeństwie hasła! Użyj zmiennych Secret.
