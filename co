RUN curl -sSLk "https://cdn.cypress.io/desktop/${CYPRESS_VERSION}/platform=linux&arch=x64.zip" \
    | unzip -q -d /usr/local/bin && \
    chmod -R 755 /usr/local/bin/cypress

# Dodanie ścieżki do Cypressa do PATH, aby móc go używać
ENV PATH="/usr/local/bin/cypress/bin:${PATH}"
