jq --arg password TEST --arg keycloak_tokenPassword TEST_2 \
  '.password |= $password | .keycloak_tokenPassword |= $keycloak_tokenPassword' \
  cypress.env.json > temp.json && mv temp.json cypress.env.jso
